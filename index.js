const token_server = require("./commands/token_server");
const pix_invoice = require("./commands/pix_invoice");
const pix_payment = require("./commands/pix_payment");

module.exports = {
    token_server,
    pix_invoice,
    pix_payment
}
